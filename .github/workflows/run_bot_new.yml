name: 🤖 ChatGPT RDP Bot Runner

on:
  schedule:
    - cron: "0 */6 * * *"   # restart mỗi 6 giờ để bot luôn sống (GitHub-hosted runner giới hạn thời gian)
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: 🧾 Checkout
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyTelegramBotAPI requests openai

      - name: 🚀 Run bot
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          GH_PAT: ${{ secrets.GH_PAT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: gpt-5-mini
          WORKFLOW_FILE: WindowsRDP.yml
          DEFAULT_LANG: "Tiếng Việt"
        run: python bot.py
